{
    "name": "<%= appname %>",
    "version": "0.0.1",
    "dependencies": {},
    "devDependencies": {
        "web-ext": "^2.4.0"
    },
    "scripts": {
      "build": "web-ext build -s extension/",
      "start": "web-ext run -s extension/"
    }
}
